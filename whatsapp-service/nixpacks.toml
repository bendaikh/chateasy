[phases.setup]
nixPkgs = [
    "chromium",
    "nss",
    "freetype",
    "freetype.dev",
    "fontconfig",
    "stdenv.cc.cc.lib"
]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "npm start"
